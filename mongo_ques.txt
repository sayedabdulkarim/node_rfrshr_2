# MongoDB Interview Questions (Beginner to Advanced)

---
## ALL QUESTIONS AT A GLANCE

BASIC (1-33)              INTERMEDIATE (34-93)           ADVANCED (94-193)
─────────────────────────────────────────────────────────────────────────────────
1.  What is MongoDB?      34. Multiple collections       94.  WiredTiger engine
2.  What is document?     35. Aggregation Pipeline       95.  Replica Set arch
3.  What is collection?   36. MongoDB Indexes            96.  Election process
4.  Default port?         37. Create indexes             97.  Write Concerns
5.  Connect Node.js?      38. Pagination                 98.  Read Concerns
6.  What is Mongoose?     39. find() vs aggregate()      99.  Read Preferences
7.  Create schema?        40. Optimize queries           100. Sharding work
8.  CRUD operations?      41. Transactions               101. Range vs Hash shard
9.  Insert document?      42. Soft Delete                102. Chunks & migration
10. HTTP Status Codes?    43. Search functionality       103. Config Server
11. Exception Handling?   44. Embed vs Reference         104. mongos router
12. Find documents?       45. Case-insensitive search    105. Shard key design
13. Update document?      46. $lookup                    106. Monotonic shard keys
14. Delete document?      47. $unwind                    107. Change Streams
15. What is _id?          48. $group                     108. Event-driven arch
16. MongoDB vs MySQL?     49. $match                     109. Oplog
17. NoSQL database?       50. $project                   110. Oplog size
18. What is Redis?        51. Join collections           111. MongoDB + Redis
19. Install MongoDB?      52. Aggregation operators      112. In-memory vs Wired
20. What is mongosh?      53. Production connection      113. Database migration
21. MongoDB data types?   54. Connection pooling         114. MongoDB Atlas
22. Show all databases?   55. $lookup vs $graphLookup    115. Atlas prod setup
23. Create database?      56. Count efficiently          116. Atlas triggers
24. Drop database?        57. delete methods             117. Atlas Search
25. Use of find()?        58. Update nested objects      118. $text vs Atlas
26. findOne() vs find()?  59. Update arrays              119. Geospatial queries
27. Limit results?        60. Schema Design patterns     120. $near & $geoWithin
28. Sort documents?       61. Replication                121. MapReduce
29. countDocuments()?     62. Sharding                   122. Data archiving
30. MongoDB running?      63. Duplicate key errors       123. Capped Collections
31. updateOne vs Many?    64. $addFields & $set          124. Audit logging
32. $set operator?        65. Sort with pagination       125. Security practices
33. $inc operator?        66. save() vs create()         126. Field encryption
                          67. insertOne vs Many          127. CSFLE
                          68. Full-text search           128. Role-based auth
                          69. TTL indexes                129. Built-in roles
                          70. Bulk operations            130. Custom roles
                          71. $facet                     131. Enable auth
                          72. lean() vs toObject()       132. SCRAM auth
                          73. Autocomplete search        133. LDAP auth
                          74. Schema best practices      134. Backup & restore
                          75. Filter nested arrays       135. mongodump vs snap
                          76. $push & $addToSet          136. Point-in-time recov
                          77. $push vs $pull             137. Journaling
                          78. Role-based access          138. Monitor performance
                          79. $bucket                    139. Production metrics
                          80. Connection failures        140. Slow queries
                          81. .explain()                 141. MongoDB profiler
                          82. Query performance          142. explain(execStats)
                          83. unique vs sparse idx       143. Covered queries
                          84. Compound indexes           144. Horizontal scaling
                          85. $regex operator            145. CAP theorem
                          86. Search+Filter+Sort         146. Consistency vs avail
                          87. Mongoose middleware        147. Eventual consistency
                          88. Schema validation          148. Split-brain
                          89. populate()                 149. MongoDB limits
                          90. Comparison operators       150. GridFS
                          91. $or and $and               151. GridFS vs BSON
                          92. $in and $nin               152. Data partitioning
                          93. Large dataset paging       153. Zone sharding
                                                         154. Standalone to replica
                                                         155. Replica to sharded
                                                         156. Zero-downtime deploy
                                                         157. Schema versioning
                                                         158. Schema migrations
                                                         159. $merge stage
                                                         160. $out stage
                                                         161. Materialized views
                                                         162. Wildcard indexes
                                                         163. Multikey vs compound
                                                         164. Time-series storage
                                                         165. Time-Series Collect
                                                         166. Optimize aggregation
                                                         167. allowDiskUse
                                                         168. Cursor pagination
                                                         169. skip-limit vs cursor
                                                         170. Concurrent writes
                                                         171. Optimistic Locking
                                                         172. findAndModify vs upd
                                                         173. Distributed locks
                                                         174. $sample stage
                                                         175. Random document
                                                         176. $expr & $function
                                                         177. JS in aggregation
                                                         178. JS performance
                                                         179. Multi-tenant arch
                                                         180. Multi-tenancy strat
                                                         181. Cross-shard transact
                                                         182. Transaction limits
                                                         183. Saga pattern
                                                         184. MongoDB Realm
                                                         185. Offline-first sync
                                                         186. Pipeline optimize
                                                         187. $planCacheStats
                                                         188. Query plan cache
                                                         189. Rebuild indexes
                                                         190. Index intersection
                                                         191. DB-per-service
                                                         192. Microservices issues
                                                         193. Event sourcing

---

## BEGINNER LEVEL

## 1. What is MongoDB? How is it different from SQL databases?

  MongoDB is a NoSQL document-oriented database that stores data in JSON (JavaScript Object Notation )-like documents (BSON ( Binary JSON) format).

  Key Differences from SQL:

  | SQL Databases                     | MongoDB                       |
  |-----------------------------------|-------------------------------|
  | Tables                            | Collections                   |
  | Rows                              | Documents                     |
  | Columns                           | Fields                        |
  | Fixed schema                      | Flexible schema               |
  | JOIN operations                   | Embedded documents or $lookup |
  | Vertical scaling                  | Horizontal scaling (sharding) |
  | ACID transactions (traditionally) | ACID transactions (4.0+)      |


## 2. What is a document in MongoDB?


  A document is a record in MongoDB, similar to a row in SQL. It's stored in BSON (Binary JSON) format.

  Example from your code (server/models/Todo.js:1-30):
  {
    _id: ObjectId("507f1f77bcf86cd799439011"),
    user: ObjectId("507f191e810c19729de860ea"),
    text: "Buy groceries",
    completed: false,
    createdAt: ISODate("2025-01-15T10:30:00Z"),
    updatedAt: ISODate("2025-01-15T10:30:00Z")
  }

## 3. What is a collection in MongoDB?

A collection is a group of documents, similar to a table in SQL. Collections don't enforce a schema by default (flexible
  structure).

  In your app, you have 3 collections:
  - users - Stores user accounts
  - todos - Stores todo items
  - roles - Stores role definitions with permissions

## 4. What is the default port of MongoDB?

  27017 is the default port for MongoDB.

## 5. How to connect MongoDB with Node.js?

 const mongoose = require('mongoose');

  const connectDB = async () => {
    try {
      await mongoose.connect(process.env.MONGODB_URI);
      console.log('MongoDB Connected...');
    } catch (err) {
      console.error('MongoDB connection error:', err.message);
      process.exit(1);
    }
  };

## 6. What is Mongoose? Why do we use it?

 Mongoose is an ODM (Object Data Modeling) library for MongoDB and Node.js.

  Benefits:
  - Schema validation
  - Middleware/hooks (pre/post operations)
  - Built-in type casting
  - Query building helpers
  - Relationship management (populate)
  - Data validation

  In your app: You use Mongoose for all database operations, with schemas for User, Todo, and Role models.

## 7. How to create a schema in Mongoose?

const mongoose = require('mongoose');

  const userSchema = new mongoose.Schema({
    name: {
      type: String,
      required: true,
    },
    createdAt: {
      type: Date,
      default: Date.now,
    },
  });

  const User = mongoose.model('User', userSchema);

## 8. What are the basic CRUD operations in MongoDB?

  CRUD = Create, Read, Update, Delete

  | Operation | MongoDB Method            | Mongoose Method                              |
  |-----------|---------------------------|----------------------------------------------|
  | Create    | insertOne(), insertMany() | Model.create(), new Model().save()           |
  | Read      | find(), findOne()         | Model.find(), Model.findById()               |
  | Update    | updateOne(), updateMany() | Model.findByIdAndUpdate(), Model.updateOne() |
  | Delete    | deleteOne(), deleteMany() | Model.findByIdAndDelete(), Model.deleteOne() |

## 9. How to insert a document in MongoDB? ( by using create )

router.post('/', protect, async (req, res) => {
    try {
      const todo = await Todo.create({
        user: req.user._id,
        text: req.body.text,
        completed: false,
      });
      res.status(201).json(todo);
    } catch (error) {
      res.status(400).json({ message: error.message });
    }
  });

## 10. What are HTTP Status Codes? Why are they important?

Status codes tell the client what happened with the request - success, fail, or why it failed.

| Range | Meaning | Examples |
|-------|---------|----------|
| 2xx | ✅ Success | 200 OK, 201 Created, 204 No Content |
| 3xx | ↪️ Redirect | 301 Moved, 304 Not Modified |
| 4xx | ❌ Client Error | 400 Bad Request, 401 Unauthorized, 404 Not Found, 409 Conflict |
| 5xx | ❌ Server Error | 500 Internal Server Error, 503 Service Unavailable |

**Common Status Codes:**
- **200** - OK (Success)
- **201** - Created (New resource created)
- **400** - Bad Request (Invalid data)
- **401** - Unauthorized (No/Invalid token)
- **403** - Forbidden (No permission)
- **404** - Not Found
- **409** - Conflict (Duplicate entry)
- **500** - Internal Server Error

**Duplicate Entry:** Use `409 Conflict` or `400 Bad Request`
```javascript
if (err.code === 11000) {
  res.status(409).json({ error: "Email already exists" });
}
```

## 11. What is Exception Handling? Why is it important?

Exception handling se app crash hone se bachti hai. Jab unexpected errors aate hain jaise invalid input, database failure, ya network issues - inhe properly handle karke user ko meaningful message dete hain aur app stable rehti hai. Try-catch aur global error handler use karte hain.

## 12. How to find documents in MongoDB?
   - find / findById

## 13. How to update a document in MongoDB?
   - findByIdAndUpdate(), findOneAndUpdate(), updateOne(), updateMany()

## 14. How to delete a document in MongoDB?

deleteOne, deleteMany, findByIdAndDelete, findOneAndDelete

## 15. What is _id in MongoDB? Is it mandatory?
   - It is an Unique identifier for each document
   - _id = Mandatory ✅
   - Auto-generated if not provided ✅
   - Always unique ✅

   Benefits
   ---
   - Always unique
   - Sortable by creation time
   - No collision risk

## 16. What is the difference between MongoDB and MySQL?

MongoDB vs MySQL

  | Feature        | MongoDB                             | MySQL                            |
  |----------------|-------------------------------------|----------------------------------|
  | Type           | NoSQL (Document DB)                 | SQL (Relational DB)              |
  | Data Storage   | Collections → Documents (JSON/BSON) | Tables → Rows                    |
  | Schema         | Flexible (schema-less)              | Fixed (strict schema)            |
  | Relationships  | Embedded docs or $lookup            | JOINs (foreign keys)             |
  | Scaling        | Horizontal (sharding)               | Vertical (bigger server)         |
  | Query Language | MongoDB Query Language              | SQL                              |
  | ACID           | Yes (4.0+, single doc always)       | Yes (full support)               |
  | Use Case       | Flexible data, rapid changes        | Structured data, complex queries |
  | Performance    | Fast reads/writes (denormalized)    | Fast with proper indexing        |
  | Example        | User profiles, logs, real-time      | Banking, inventory, accounting   |


## 17. What is NoSQL database? Give examples. ( refer Question : 1 )

## 18. What is Redis?
   Redis is an in-memory key-value database (NoSQL) used primarily for caching.
   - We use Redis for cache data only, not for original database
   - Data stored in RAM (super fast but expensive)
   - In case server restarts, we lose all Redis data (unless persistence enabled)
   - MongoDB = Permanent storage, Redis = Temporary cache

## 19. How to install MongoDB on your system?

## 20. What is mongosh (MongoDB Shell)?

## 21. What are MongoDB data types?

MongoDB supports various BSON data types:

| Data Type     | Description                          | Example                              |
|---------------|--------------------------------------|--------------------------------------|
| String        | UTF-8 text                           | `"Hello World"`                      |
| Integer       | 32-bit or 64-bit numbers             | `42`, `NumberInt(42)`                |
| Double        | 64-bit floating point                | `3.14`                               |
| Boolean       | true or false                        | `true`, `false`                      |
| ObjectId      | 12-byte unique identifier            | `ObjectId("507f1f77bcf86cd799439011")` |
| Date          | Date/time in milliseconds            | `ISODate("2025-01-15T10:30:00Z")`    |
| Array         | List of values                       | `["apple", "banana", "cherry"]`      |
| Object        | Embedded document                    | `{ name: "John", age: 30 }`          |
| Null          | Null/empty value                     | `null`                               |
| Binary Data   | Binary data (images, files)          | `BinData(0, "base64string")`         |
| Decimal128    | High-precision decimal               | `NumberDecimal("19.99")`             |
| Timestamp     | Internal MongoDB timestamp           | `Timestamp(1234567890, 1)`           |
| RegExp        | Regular expression                   | `/pattern/i`                         |

## 22. How to show all databases in MongoDB?

```javascript
// In mongosh:
show dbs

// Or using command:
db.adminCommand({ listDatabases: 1 })
```

**Output Example:**
```
admin    40.00 KiB
config   60.00 KiB
local    40.00 KiB
myapp    85.00 KiB
```

**Note:** Empty databases (with no collections) won't show in the list.

## 23. How to create a database in MongoDB?

## 24. How to drop a database in MongoDB?

## 25. What is the use of .find() method?

## 26. What is the difference between findOne() and find()?

## 27. How to limit results in MongoDB query?

```javascript
// Top 5 users lao
const users = await User.find().limit(5)

// Latest 10 todos
const todos = await Todo.find().sort({ createdAt: -1 }).limit(10)
```

**Pagination mein use:**
```javascript
const page = 2;
const limit = 10;
const skip = (page - 1) * limit;  // 10

const users = await User.find()
  .skip(skip)      // Pehle 10 skip
  .limit(limit)    // Next 10 lao
```

| Page | Query | Result |
|------|-------|--------|
| 1 | skip(0).limit(10) | 1-10 |
| 2 | skip(10).limit(10) | 11-20 |
| 3 | skip(20).limit(10) | 21-30 |

## 28. How to sort documents in MongoDB?

```javascript
// Ascending (A-Z, 1-100)
const users = await User.find().sort({ name: 1 })

// Descending (Z-A, 100-1)
const users = await User.find().sort({ name: -1 })

// Multiple fields
const todos = await Todo.find().sort({ completed: 1, createdAt: -1 })
```

| Value | Matlab |
|-------|--------|
| `1` | Ascending (chhota → bada) |
| `-1` | Descending (bada → chhota) |

**Shorthand syntax:**
```javascript
.sort('-createdAt')  // Same as { createdAt: -1 }
.sort('name')        // Same as { name: 1 }
```

## 29. What is the use of .countDocuments()?

Total documents count karta hai.

```javascript
// Saare users count karo
const total = await User.countDocuments()  // 150

// With filter - active users count
const activeUsers = await User.countDocuments({ isActive: true })  // 89

// User ke todos count
const todoCount = await Todo.countDocuments({ user: req.user._id })
const completedCount = await Todo.countDocuments({ user: req.user._id, completed: true })
```

**Pagination mein use:**
```javascript
const total = await Todo.countDocuments({ user: req.user._id })
const totalPages = Math.ceil(total / limit)
```

## 30. How to check if MongoDB is running?

## 31. What is the difference between updateOne() and updateMany()?

## 32. What is the use of $set operator?

## 33. What is the use of $inc operator?

---

## INTERMEDIATE LEVEL

## 34. How to fetch data from multiple collections (userList, order, productList) in a single query?
  Ans: refer 35 answer.

## 35. What is MongoDB Aggregation Pipeline? Explain stages.

 Ans:  if we want to do multi operation within a single query , we will use aggregator

⏺ 100% Sahi bola bhai!

  Simple Rule:

  | Situation                           | Method      |
  |-------------------------------------|-------------|
  | Single operation (fetch, filter)    | find()      |
  | Multiple operations in single query | aggregate() |

  Aggregate = Pipeline (ek ke baad ek operation)

  Order.aggregate([
    { $match: ... },    // Step 1: Filter
    { $lookup: ... },   // Step 2: Join
    { $group: ... },    // Step 3: Group
    { $sort: ... },     // Step 4: Sort
    { $project: ... }   // Step 5: Select fields
  ])

  Data ek step se nikalta hai → next step mein jaata hai → final result

  Interview mein bol:

  "Jab multiple operations jaise filtering, joining, grouping, sorting ek saath karni ho single query mein, tab aggregation pipeline
   use karte hain. Ye SQL ke complex queries jaisa hai with JOINs, GROUP BY, ORDER BY sab ek mein."


## 36. What are MongoDB Indexes? Why are they important for performance?

## 37. How to create indexes in MongoDB? What types exist?

## 38. How to implement pagination in MongoDB (with and without aggregation)?

## 39. What is the difference between find() and aggregate()?

## 40. How to optimize MongoDB queries for better performance?

## 41. What are MongoDB Transactions? When to use them?

## 42. What is Soft Delete? How to implement it?

## 43. How to implement search functionality in MongoDB?

## 44. What is the difference between embedding and referencing in MongoDB?

## 45. How to perform case-insensitive search in MongoDB?

## 46. What is $lookup in MongoDB? How to use it?

## 47. What is $unwind in aggregation? When to use it?

## 48. What is $group in aggregation? Give example.

## 49. What is $match in aggregation pipeline?

## 50. What is $project in aggregation? How is it different from .select()?

## 51. How to join 2 or more collections in MongoDB?

## 52. What are aggregation operators? ($sum, $avg, $min, $max, etc.)

## 53. How to handle MongoDB connection in production?

## 54. What is connection pooling in MongoDB?

## 55. What is the difference between $lookup and $graphLookup?

## 56. How to count documents efficiently in MongoDB?

## 57. What is the difference between deleteOne(), deleteMany(), and findOneAndDelete()?

## 58. How to update nested objects in MongoDB?

## 59. How to update arrays in MongoDB documents?

## 60. What are MongoDB Schema Design patterns?

## 61. What is MongoDB Replication?

## 62. What is MongoDB Sharding?

## 63. How to handle duplicate key errors in MongoDB?

## 64. What is $addFields and $set in aggregation?

## 65. How to implement sorting with pagination?

## 66. What is the difference between save() and create() in Mongoose?

## 67. What is the difference between insertOne() and insertMany()?

## 68. How to implement full-text search in MongoDB?

## 69. What are TTL (Time To Live) indexes? When to use them?

## 70. How to perform bulk operations in MongoDB?

## 71. What is $facet in aggregation? Give use case.

## 72. What is the difference between lean() and toObject() in Mongoose?

## 73. How to implement autocomplete/suggest search in MongoDB?

## 74. What are the best practices for MongoDB schema design?

## 75. How to filter nested arrays in MongoDB?

## 76. How to use $push and $addToSet operators?

## 77. What is the difference between $push and $pull?

## 78. How to implement role-based access control in MongoDB?

## 79. What is $bucket and $bucketAuto in aggregation?

## 80. How to handle MongoDB connection failures in production?

## 81. What is the use of .explain() in MongoDB?

## 82. How to check query performance in MongoDB?

## 83. What is the difference between unique index and sparse index?

## 84. How to create compound indexes in MongoDB?

## 85. What is the use of $regex operator?

## 86. How to implement search, filter, sort, pagination in single API?

## 87. What are Mongoose middleware/hooks? (pre, post)

## 88. How to validate data in Mongoose schema?

## 89. What is populate() in Mongoose? How does it work?

## 90. What is the difference between $eq, $ne, $gt, $gte, $lt, $lte?

## 91. How to use $or and $and operators in MongoDB?

## 92. What is the use of $in and $nin operators?

## 93. How to handle pagination for large datasets efficiently?

---

## ADVANCED LEVEL

## 94. What is WiredTiger storage engine in MongoDB? How does it work?

## 95. Explain MongoDB Replica Set architecture (Primary, Secondary, Arbiter).

## 96. How does MongoDB election process work when primary goes down?

## 97. What are Write Concerns in MongoDB? (w:1, w:majority, w:0)

## 98. What are Read Concerns in MongoDB? (local, majority, linearizable)

## 99. What are Read Preferences in MongoDB? (primary, secondary, nearest)

## 100. How does MongoDB Sharding work? Explain shard key selection.

## 101. What is the difference between Range-based and Hash-based sharding?

## 102. What are Chunks in MongoDB sharding? How does chunk migration work?

## 103. What is a Config Server in MongoDB sharded cluster?

## 104. What is mongos router in MongoDB? How does it work?

## 105. How to design a shard key for high cardinality and even distribution?

## 106. What are the problems with monotonically increasing shard keys?

## 107. What is Change Streams in MongoDB? How to implement real-time updates?

## 108. How to implement event-driven architecture using Change Streams?

## 109. What is Oplog in MongoDB? How is it used in replication?

## 110. What is the Oplog size limit? How to configure it?

## 111. How to implement MongoDB caching with Redis?

## 112. What is the difference between in-memory storage and WiredTiger?

## 113. How to implement database migration in MongoDB?

## 114. What is MongoDB Atlas? Explain its features.

## 115. How to set up MongoDB Atlas cluster for production?

## 116. What are MongoDB Atlas triggers and functions?

## 117. How to implement full-text search with Atlas Search?

## 118. What is the difference between $text search and Atlas Search?

## 119. How to implement geospatial queries in MongoDB? (2d, 2dsphere indexes)

## 120. How to find nearby locations using $near and $geoWithin?

## 121. What is MapReduce in MongoDB? When to use it vs Aggregation?

## 122. How to implement data archiving strategy in MongoDB?

## 123. What are Capped Collections in MongoDB? When to use them?

## 124. How to implement audit logging in MongoDB?

## 125. What are MongoDB security best practices?

## 126. How to implement field-level encryption in MongoDB?

## 127. What is Client-Side Field Level Encryption (CSFLE)?

## 128. How to implement MongoDB role-based authentication?

## 129. What are MongoDB built-in roles? (read, readWrite, dbAdmin, etc.)

## 130. How to create custom roles in MongoDB?

## 131. How to enable and configure MongoDB authentication?

## 132. What is SCRAM authentication in MongoDB?

## 133. How to implement LDAP authentication with MongoDB?

## 134. How to backup and restore MongoDB database?

## 135. What is the difference between mongodump and mongorestore vs file system snapshots?

## 136. How to implement point-in-time recovery in MongoDB?

## 137. What is journaling in MongoDB? How does it ensure durability?

## 138. How to monitor MongoDB performance? (mongostat, mongotop)

## 139. What metrics to monitor in MongoDB production environment?

## 140. How to identify and fix slow queries in MongoDB?

## 141. What is the MongoDB profiler? How to use it?

## 142. How to analyze query plans using explain("executionStats")?

## 143. What are covered queries in MongoDB? How to optimize for them?

## 144. How to implement horizontal scaling in MongoDB?

## 145. What is the CAP theorem? Where does MongoDB fit?

## 146. How does MongoDB handle consistency vs availability trade-offs?

## 147. What is eventual consistency in MongoDB replication?

## 148. How to handle split-brain scenario in MongoDB?

## 149. What are MongoDB limits? (document size, nesting depth, etc.)

## 150. How to handle large files in MongoDB using GridFS?

## 151. What is the difference between GridFS and storing files as BSON?

## 152. How to implement data partitioning strategies in MongoDB?

## 153. What is zone sharding in MongoDB? How to implement it?

## 154. How to migrate from standalone MongoDB to replica set?

## 155. How to migrate from replica set to sharded cluster?

## 156. How to implement zero-downtime deployment with MongoDB?

## 157. What are the best practices for MongoDB schema versioning?

## 158. How to handle schema migrations in production MongoDB?

## 159. What is $merge stage in aggregation? How to use it for materialized views?

## 160. What is $out stage in aggregation? Difference from $merge?

## 161. How to implement materialized views in MongoDB?

## 162. What are Wildcard indexes in MongoDB? When to use them?

## 163. What is the difference between multikey index and compound index?

## 164. How to implement time-series data storage in MongoDB?

## 165. What are Time-Series Collections in MongoDB 5.0+?

## 166. How to optimize aggregation pipeline for large datasets?

## 167. What is allowDiskUse in aggregation? When to use it?

## 168. How to implement cursor-based pagination for better performance?

## 169. What is the difference between skip-limit and cursor-based pagination?

## 170. How to handle concurrent writes in MongoDB?

## 171. What is Optimistic Locking in MongoDB? How to implement it?

## 172. What is the difference between findAndModify and updateOne?

## 173. How to implement distributed locks using MongoDB?

## 174. What is $sample stage in aggregation? Use cases?

## 175. How to implement random document selection efficiently?

## 176. What are $expr and $function in aggregation?

## 177. How to use JavaScript in MongoDB aggregation ($function, $accumulator)?

## 178. What is the performance impact of using JavaScript in queries?

## 179. How to implement multi-tenant architecture in MongoDB?

## 180. What are the strategies for multi-tenancy? (shared DB, separate DB, hybrid)

## 181. How to handle cross-shard transactions in MongoDB?

## 182. What are the limitations of MongoDB transactions?

## 183. How to implement saga pattern for distributed transactions?

## 184. What is MongoDB Realm? How does it work with mobile apps?

## 185. How to implement offline-first sync with MongoDB Realm?

## 186. What are aggregation pipeline optimizations done by MongoDB internally?

## 187. How to use $planCacheStats to analyze query plans?

## 188. What is the query plan cache in MongoDB?

## 189. How to clear and rebuild indexes in MongoDB?

## 190. What is index intersection in MongoDB?

## 191. How to implement database-per-service pattern with MongoDB?

## 192. What are the challenges of microservices with MongoDB?

## 193. How to implement event sourcing with MongoDB?
