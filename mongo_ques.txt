# MongoDB Interview Questions (Beginner to Intermediate)

## BEGINNER LEVEL

## 1. What is MongoDB? How is it different from SQL databases?

  MongoDB is a NoSQL document-oriented database that stores data in JSON (JavaScript Object Notation )-like documents (BSON ( Binary JSON) format).

  Key Differences from SQL:

  | SQL Databases                     | MongoDB                       |
  |-----------------------------------|-------------------------------|
  | Tables                            | Collections                   |
  | Rows                              | Documents                     |
  | Columns                           | Fields                        |
  | Fixed schema                      | Flexible schema               |
  | JOIN operations                   | Embedded documents or $lookup |
  | Vertical scaling                  | Horizontal scaling (sharding) |
  | ACID transactions (traditionally) | ACID transactions (4.0+)      |


## 2. What is a document in MongoDB?


  A document is a record in MongoDB, similar to a row in SQL. It's stored in BSON (Binary JSON) format.

  Example from your code (server/models/Todo.js:1-30):
  {
    _id: ObjectId("507f1f77bcf86cd799439011"),
    user: ObjectId("507f191e810c19729de860ea"),
    text: "Buy groceries",
    completed: false,
    createdAt: ISODate("2025-01-15T10:30:00Z"),
    updatedAt: ISODate("2025-01-15T10:30:00Z")
  }

## 3. What is a collection in MongoDB?

A collection is a group of documents, similar to a table in SQL. Collections don't enforce a schema by default (flexible
  structure).

  In your app, you have 3 collections:
  - users - Stores user accounts
  - todos - Stores todo items
  - roles - Stores role definitions with permissions

## 4. What is the default port of MongoDB?

  27017 is the default port for MongoDB.

## 5. How to connect MongoDB with Node.js?

 const mongoose = require('mongoose');

  const connectDB = async () => {
    try {
      await mongoose.connect(process.env.MONGODB_URI);
      console.log('MongoDB Connected...');
    } catch (err) {
      console.error('MongoDB connection error:', err.message);
      process.exit(1);
    }
  };

## 6. What is Mongoose? Why do we use it?

 Mongoose is an ODM (Object Data Modeling) library for MongoDB and Node.js.

  Benefits:
  - Schema validation
  - Middleware/hooks (pre/post operations)
  - Built-in type casting
  - Query building helpers
  - Relationship management (populate)
  - Data validation

  In your app: You use Mongoose for all database operations, with schemas for User, Todo, and Role models.

## 7. How to create a schema in Mongoose?

const mongoose = require('mongoose');

  const userSchema = new mongoose.Schema({
    name: {
      type: String,
      required: true,
    },
    createdAt: {
      type: Date,
      default: Date.now,
    },
  });

  const User = mongoose.model('User', userSchema);

## 8. What are the basic CRUD operations in MongoDB?

  CRUD = Create, Read, Update, Delete

  | Operation | MongoDB Method            | Mongoose Method                              |
  |-----------|---------------------------|----------------------------------------------|
  | Create    | insertOne(), insertMany() | Model.create(), new Model().save()           |
  | Read      | find(), findOne()         | Model.find(), Model.findById()               |
  | Update    | updateOne(), updateMany() | Model.findByIdAndUpdate(), Model.updateOne() |
  | Delete    | deleteOne(), deleteMany() | Model.findByIdAndDelete(), Model.deleteOne() |

## 9. How to insert a document in MongoDB? ( by using create )

router.post('/', protect, async (req, res) => {
    try {
      const todo = await Todo.create({
        user: req.user._id,
        text: req.body.text,
        completed: false,
      });
      res.status(201).json(todo);
    } catch (error) {
      res.status(400).json({ message: error.message });
    }
  });

## 10. What are HTTP Status Codes? Why are they important?

Status codes tell the client what happened with the request - success, fail, or why it failed.

| Range | Meaning | Examples |
|-------|---------|----------|
| 2xx | ✅ Success | 200 OK, 201 Created, 204 No Content |
| 3xx | ↪️ Redirect | 301 Moved, 304 Not Modified |
| 4xx | ❌ Client Error | 400 Bad Request, 401 Unauthorized, 404 Not Found, 409 Conflict |
| 5xx | ❌ Server Error | 500 Internal Server Error, 503 Service Unavailable |

**Common Status Codes:**
- **200** - OK (Success)
- **201** - Created (New resource created)
- **400** - Bad Request (Invalid data)
- **401** - Unauthorized (No/Invalid token)
- **403** - Forbidden (No permission)
- **404** - Not Found
- **409** - Conflict (Duplicate entry)
- **500** - Internal Server Error

**Duplicate Entry:** Use `409 Conflict` or `400 Bad Request`
```javascript
if (err.code === 11000) {
  res.status(409).json({ error: "Email already exists" });
}
```

## 11. How to find documents in MongoDB?
   - find / findById

## 12. How to update a document in MongoDB?
   - findByIdAndUpdate(), findOneAndUpdate(), updateOne(), updateMany()

## 13. How to delete a document in MongoDB?

deleteOne, deleteMany, findByIdAndDelete, findOneAndDelete

## 14. What is _id in MongoDB? Is it mandatory?
   - It is an Unique identifier for each document
   - _id = Mandatory ✅
   - Auto-generated if not provided ✅
   - Always unique ✅

   Benefits
   ---
   - Always unique
   - Sortable by creation time
   - No collision risk

## 15. What is the difference between MongoDB and MySQL?

MongoDB vs MySQL

  | Feature        | MongoDB                             | MySQL                            |
  |----------------|-------------------------------------|----------------------------------|
  | Type           | NoSQL (Document DB)                 | SQL (Relational DB)              |
  | Data Storage   | Collections → Documents (JSON/BSON) | Tables → Rows                    |
  | Schema         | Flexible (schema-less)              | Fixed (strict schema)            |
  | Relationships  | Embedded docs or $lookup            | JOINs (foreign keys)             |
  | Scaling        | Horizontal (sharding)               | Vertical (bigger server)         |
  | Query Language | MongoDB Query Language              | SQL                              |
  | ACID           | Yes (4.0+, single doc always)       | Yes (full support)               |
  | Use Case       | Flexible data, rapid changes        | Structured data, complex queries |
  | Performance    | Fast reads/writes (denormalized)    | Fast with proper indexing        |
  | Example        | User profiles, logs, real-time      | Banking, inventory, accounting   |


## 16. What is NoSQL database? Give examples. ( refer Question : 1 )

## 17. What is Redis?
   Redis is an in-memory key-value database (NoSQL) used primarily for caching.
   - We use Redis for cache data only, not for original database
   - Data stored in RAM (super fast but expensive)
   - In case server restarts, we lose all Redis data (unless persistence enabled)
   - MongoDB = Permanent storage, Redis = Temporary cache

## 18. How to install MongoDB on your system?

## 19. What is mongosh (MongoDB Shell)?

## 20. What are MongoDB data types?

MongoDB supports various BSON data types:

| Data Type     | Description                          | Example                              |
|---------------|--------------------------------------|--------------------------------------|
| String        | UTF-8 text                           | `"Hello World"`                      |
| Integer       | 32-bit or 64-bit numbers             | `42`, `NumberInt(42)`                |
| Double        | 64-bit floating point                | `3.14`                               |
| Boolean       | true or false                        | `true`, `false`                      |
| ObjectId      | 12-byte unique identifier            | `ObjectId("507f1f77bcf86cd799439011")` |
| Date          | Date/time in milliseconds            | `ISODate("2025-01-15T10:30:00Z")`    |
| Array         | List of values                       | `["apple", "banana", "cherry"]`      |
| Object        | Embedded document                    | `{ name: "John", age: 30 }`          |
| Null          | Null/empty value                     | `null`                               |
| Binary Data   | Binary data (images, files)          | `BinData(0, "base64string")`         |
| Decimal128    | High-precision decimal               | `NumberDecimal("19.99")`             |
| Timestamp     | Internal MongoDB timestamp           | `Timestamp(1234567890, 1)`           |
| RegExp        | Regular expression                   | `/pattern/i`                         |

## 21. How to show all databases in MongoDB?

```javascript
// In mongosh:
show dbs

// Or using command:
db.adminCommand({ listDatabases: 1 })
```

**Output Example:**
```
admin    40.00 KiB
config   60.00 KiB
local    40.00 KiB
myapp    85.00 KiB
```

**Note:** Empty databases (with no collections) won't show in the list.

## 22. How to create a database in MongoDB?

## 23. How to drop a database in MongoDB?

## 24. What is the use of .find() method?

## 25. What is the difference between findOne() and find()?

## 26. How to limit results in MongoDB query?

## 27. How to sort documents in MongoDB?

## 28. What is the use of .countDocuments()?

## 29. How to check if MongoDB is running?

## 30. What is the difference between updateOne() and updateMany()?

## 31. What is the use of $set operator?

## 32. What is the use of $inc operator?

---

## INTERMEDIATE LEVEL

## 33. How to fetch data from multiple collections (userList, order, productList) in a single query?
  Ans: refer 34 answer.

## 34. What is MongoDB Aggregation Pipeline? Explain stages.

 Ans:  if we want to do multi operation within a single query , we will use aggregator

⏺ 100% Sahi bola bhai!

  Simple Rule:

  | Situation                           | Method      |
  |-------------------------------------|-------------|
  | Single operation (fetch, filter)    | find()      |
  | Multiple operations in single query | aggregate() |

  Aggregate = Pipeline (ek ke baad ek operation)

  Order.aggregate([
    { $match: ... },    // Step 1: Filter
    { $lookup: ... },   // Step 2: Join
    { $group: ... },    // Step 3: Group
    { $sort: ... },     // Step 4: Sort
    { $project: ... }   // Step 5: Select fields
  ])

  Data ek step se nikalta hai → next step mein jaata hai → final result

  Interview mein bol:

  "Jab multiple operations jaise filtering, joining, grouping, sorting ek saath karni ho single query mein, tab aggregation pipeline
   use karte hain. Ye SQL ke complex queries jaisa hai with JOINs, GROUP BY, ORDER BY sab ek mein."


## 35. What are MongoDB Indexes? Why are they important for performance?

## 36. How to create indexes in MongoDB? What types exist?

## 37. How to implement pagination in MongoDB (with and without aggregation)?

## 38. What is the difference between find() and aggregate()?

## 39. How to optimize MongoDB queries for better performance?

## 40. What are MongoDB Transactions? When to use them?

## 41. What is Soft Delete? How to implement it?

## 42. How to implement search functionality in MongoDB?

## 43. What is the difference between embedding and referencing in MongoDB?

## 44. How to perform case-insensitive search in MongoDB?

## 45. What is $lookup in MongoDB? How to use it?

## 46. What is $unwind in aggregation? When to use it?

## 47. What is $group in aggregation? Give example.

## 48. What is $match in aggregation pipeline?

## 49. What is $project in aggregation? How is it different from .select()?

## 50. How to join 2 or more collections in MongoDB?

## 51. What are aggregation operators? ($sum, $avg, $min, $max, etc.)

## 52. How to handle MongoDB connection in production?

## 53. What is connection pooling in MongoDB?

## 54. What is the difference between $lookup and $graphLookup?

## 55. How to count documents efficiently in MongoDB?

## 56. What is the difference between deleteOne(), deleteMany(), and findOneAndDelete()?

## 57. How to update nested objects in MongoDB?

## 58. How to update arrays in MongoDB documents?

## 59. What are MongoDB Schema Design patterns?

## 60. What is MongoDB Replication?

## 61. What is MongoDB Sharding?

## 62. How to handle duplicate key errors in MongoDB?

## 63. What is $addFields and $set in aggregation?

## 64. How to implement sorting with pagination?

## 65. What is the difference between save() and create() in Mongoose?

## 66. What is the difference between insertOne() and insertMany()?

## 67. How to implement full-text search in MongoDB?

## 68. What are TTL (Time To Live) indexes? When to use them?

## 69. How to perform bulk operations in MongoDB?

## 70. What is $facet in aggregation? Give use case.

## 71. What is the difference between lean() and toObject() in Mongoose?

## 72. How to implement autocomplete/suggest search in MongoDB?

## 73. What are the best practices for MongoDB schema design?

## 74. How to filter nested arrays in MongoDB?

## 75. How to use $push and $addToSet operators?

## 76. What is the difference between $push and $pull?

## 77. How to implement role-based access control in MongoDB?

## 78. What is $bucket and $bucketAuto in aggregation?

## 79. How to handle MongoDB connection failures in production?

## 80. What is the use of .explain() in MongoDB?

## 81. How to check query performance in MongoDB?

## 82. What is the difference between unique index and sparse index?

## 83. How to create compound indexes in MongoDB?

## 84. What is the use of $regex operator?

## 85. How to implement search, filter, sort, pagination in single API?

## 86. What are Mongoose middleware/hooks? (pre, post)

## 87. How to validate data in Mongoose schema?

## 88. What is populate() in Mongoose? How does it work?

## 89. What is the difference between $eq, $ne, $gt, $gte, $lt, $lte?

## 90. How to use $or and $and operators in MongoDB?

## 91. What is the use of $in and $nin operators?

## 92. How to handle pagination for large datasets efficiently?

---

## ADVANCED LEVEL

## 93. What is WiredTiger storage engine in MongoDB? How does it work?

## 94. Explain MongoDB Replica Set architecture (Primary, Secondary, Arbiter).

## 95. How does MongoDB election process work when primary goes down?

## 96. What are Write Concerns in MongoDB? (w:1, w:majority, w:0)

## 97. What are Read Concerns in MongoDB? (local, majority, linearizable)

## 98. What are Read Preferences in MongoDB? (primary, secondary, nearest)

## 99. How does MongoDB Sharding work? Explain shard key selection.

## 100. What is the difference between Range-based and Hash-based sharding?

## 101. What are Chunks in MongoDB sharding? How does chunk migration work?

## 102. What is a Config Server in MongoDB sharded cluster?

## 103. What is mongos router in MongoDB? How does it work?

## 104. How to design a shard key for high cardinality and even distribution?

## 105. What are the problems with monotonically increasing shard keys?

## 106. What is Change Streams in MongoDB? How to implement real-time updates?

## 107. How to implement event-driven architecture using Change Streams?

## 108. What is Oplog in MongoDB? How is it used in replication?

## 109. What is the Oplog size limit? How to configure it?

## 110. How to implement MongoDB caching with Redis?

## 111. What is the difference between in-memory storage and WiredTiger?

## 112. How to implement database migration in MongoDB?

## 113. What is MongoDB Atlas? Explain its features.

## 114. How to set up MongoDB Atlas cluster for production?

## 115. What are MongoDB Atlas triggers and functions?

## 116. How to implement full-text search with Atlas Search?

## 117. What is the difference between $text search and Atlas Search?

## 118. How to implement geospatial queries in MongoDB? (2d, 2dsphere indexes)

## 119. How to find nearby locations using $near and $geoWithin?

## 120. What is MapReduce in MongoDB? When to use it vs Aggregation?

## 121. How to implement data archiving strategy in MongoDB?

## 122. What are Capped Collections in MongoDB? When to use them?

## 123. How to implement audit logging in MongoDB?

## 124. What are MongoDB security best practices?

## 125. How to implement field-level encryption in MongoDB?

## 126. What is Client-Side Field Level Encryption (CSFLE)?

## 127. How to implement MongoDB role-based authentication?

## 128. What are MongoDB built-in roles? (read, readWrite, dbAdmin, etc.)

## 129. How to create custom roles in MongoDB?

## 130. How to enable and configure MongoDB authentication?

## 131. What is SCRAM authentication in MongoDB?

## 132. How to implement LDAP authentication with MongoDB?

## 133. How to backup and restore MongoDB database?

## 134. What is the difference between mongodump and mongorestore vs file system snapshots?

## 135. How to implement point-in-time recovery in MongoDB?

## 136. What is journaling in MongoDB? How does it ensure durability?

## 137. How to monitor MongoDB performance? (mongostat, mongotop)

## 138. What metrics to monitor in MongoDB production environment?

## 139. How to identify and fix slow queries in MongoDB?

## 140. What is the MongoDB profiler? How to use it?

## 141. How to analyze query plans using explain("executionStats")?

## 142. What are covered queries in MongoDB? How to optimize for them?

## 143. How to implement horizontal scaling in MongoDB?

## 144. What is the CAP theorem? Where does MongoDB fit?

## 145. How does MongoDB handle consistency vs availability trade-offs?

## 146. What is eventual consistency in MongoDB replication?

## 147. How to handle split-brain scenario in MongoDB?

## 148. What are MongoDB limits? (document size, nesting depth, etc.)

## 149. How to handle large files in MongoDB using GridFS?

## 150. What is the difference between GridFS and storing files as BSON?

## 151. How to implement data partitioning strategies in MongoDB?

## 152. What is zone sharding in MongoDB? How to implement it?

## 153. How to migrate from standalone MongoDB to replica set?

## 154. How to migrate from replica set to sharded cluster?

## 155. How to implement zero-downtime deployment with MongoDB?

## 156. What are the best practices for MongoDB schema versioning?

## 157. How to handle schema migrations in production MongoDB?

## 158. What is $merge stage in aggregation? How to use it for materialized views?

## 159. What is $out stage in aggregation? Difference from $merge?

## 160. How to implement materialized views in MongoDB?

## 161. What are Wildcard indexes in MongoDB? When to use them?

## 162. What is the difference between multikey index and compound index?

## 163. How to implement time-series data storage in MongoDB?

## 164. What are Time-Series Collections in MongoDB 5.0+?

## 165. How to optimize aggregation pipeline for large datasets?

## 166. What is allowDiskUse in aggregation? When to use it?

## 167. How to implement cursor-based pagination for better performance?

## 168. What is the difference between skip-limit and cursor-based pagination?

## 169. How to handle concurrent writes in MongoDB?

## 170. What is Optimistic Locking in MongoDB? How to implement it?

## 171. What is the difference between findAndModify and updateOne?

## 172. How to implement distributed locks using MongoDB?

## 173. What is $sample stage in aggregation? Use cases?

## 174. How to implement random document selection efficiently?

## 175. What are $expr and $function in aggregation?

## 176. How to use JavaScript in MongoDB aggregation ($function, $accumulator)?

## 177. What is the performance impact of using JavaScript in queries?

## 178. How to implement multi-tenant architecture in MongoDB?

## 179. What are the strategies for multi-tenancy? (shared DB, separate DB, hybrid)

## 180. How to handle cross-shard transactions in MongoDB?

## 181. What are the limitations of MongoDB transactions?

## 182. How to implement saga pattern for distributed transactions?

## 183. What is MongoDB Realm? How does it work with mobile apps?

## 184. How to implement offline-first sync with MongoDB Realm?

## 185. What are aggregation pipeline optimizations done by MongoDB internally?

## 186. How to use $planCacheStats to analyze query plans?

## 187. What is the query plan cache in MongoDB?

## 188. How to clear and rebuild indexes in MongoDB?

## 189. What is index intersection in MongoDB?

## 190. How to implement database-per-service pattern with MongoDB?

## 191. What are the challenges of microservices with MongoDB?

## 192. How to implement event sourcing with MongoDB?
