# MongoDB Interview Questions (Beginner to Intermediate)

## BEGINNER LEVEL

## 1. What is MongoDB? How is it different from SQL databases?

  MongoDB is a NoSQL document-oriented database that stores data in JSON-like documents (BSON format).

  Key Differences from SQL:

  | SQL Databases                     | MongoDB                       |
  |-----------------------------------|-------------------------------|
  | Tables                            | Collections                   |
  | Rows                              | Documents                     |
  | Columns                           | Fields                        |
  | Fixed schema                      | Flexible schema               |
  | JOIN operations                   | Embedded documents or $lookup |
  | Vertical scaling                  | Horizontal scaling (sharding) |
  | ACID transactions (traditionally) | ACID transactions (4.0+)      |


## 2. What is a document in MongoDB?


  A document is a record in MongoDB, similar to a row in SQL. It's stored in BSON (Binary JSON) format.

  Example from your code (server/models/Todo.js:1-30):
  {
    _id: ObjectId("507f1f77bcf86cd799439011"),
    user: ObjectId("507f191e810c19729de860ea"),
    text: "Buy groceries",
    completed: false,
    createdAt: ISODate("2025-01-15T10:30:00Z"),
    updatedAt: ISODate("2025-01-15T10:30:00Z")
  }

## 3. What is a collection in MongoDB?

A collection is a group of documents, similar to a table in SQL. Collections don't enforce a schema by default (flexible
  structure).

  In your app, you have 3 collections:
  - users - Stores user accounts
  - todos - Stores todo items
  - roles - Stores role definitions with permissions

## 4. What is the default port of MongoDB?

  27017 is the default port for MongoDB.

## 5. How to connect MongoDB with Node.js?

 const mongoose = require('mongoose');

  const connectDB = async () => {
    try {
      await mongoose.connect(process.env.MONGODB_URI);
      console.log('MongoDB Connected...');
    } catch (err) {
      console.error('MongoDB connection error:', err.message);
      process.exit(1);
    }
  };

## 6. What is Mongoose? Why do we use it?

 Mongoose is an ODM (Object Data Modeling) library for MongoDB and Node.js.

  Benefits:
  - Schema validation
  - Middleware/hooks (pre/post operations)
  - Built-in type casting
  - Query building helpers
  - Relationship management (populate)
  - Data validation

  In your app: You use Mongoose for all database operations, with schemas for User, Todo, and Role models.

## 7. How to create a schema in Mongoose?

const mongoose = require('mongoose');

  const userSchema = new mongoose.Schema({
    name: {
      type: String,
      required: true,
    },
    createdAt: {
      type: Date,
      default: Date.now,
    },
  });

  const User = mongoose.model('User', userSchema);

## 8. What are the basic CRUD operations in MongoDB?

  CRUD = Create, Read, Update, Delete

  | Operation | MongoDB Method            | Mongoose Method                              |
  |-----------|---------------------------|----------------------------------------------|
  | Create    | insertOne(), insertMany() | Model.create(), new Model().save()           |
  | Read      | find(), findOne()         | Model.find(), Model.findById()               |
  | Update    | updateOne(), updateMany() | Model.findByIdAndUpdate(), Model.updateOne() |
  | Delete    | deleteOne(), deleteMany() | Model.findByIdAndDelete(), Model.deleteOne() |

## 9. How to insert a document in MongoDB? ( by using create )

router.post('/', protect, async (req, res) => {
    try {
      const todo = await Todo.create({
        user: req.user._id,
        text: req.body.text,
        completed: false,
      });
      res.status(201).json(todo);
    } catch (error) {
      res.status(400).json({ message: error.message });
    }
  });

## 10. How to find documents in MongoDB?
   - find / findById

## 11. How to update a document in MongoDB?

## 12. How to delete a document in MongoDB?

## 13. What is _id in MongoDB? Is it mandatory?

## 14. What is the difference between MongoDB and MySQL?

## 15. What is NoSQL database? Give examples.

## 16. How to install MongoDB on your system?

## 17. What is mongosh (MongoDB Shell)?

## 18. What are MongoDB data types?

## 19. How to show all databases in MongoDB?

## 20. How to create a database in MongoDB?

## 21. How to drop a database in MongoDB?

## 22. What is the use of .find() method?

## 23. What is the difference between findOne() and find()?

## 24. How to limit results in MongoDB query?

## 25. How to sort documents in MongoDB?

## 26. What is the use of .countDocuments()?

## 27. How to check if MongoDB is running?

## 28. What is the difference between updateOne() and updateMany()?

## 29. What is the use of $set operator?

## 30. What is the use of $inc operator?

---

## INTERMEDIATE LEVEL

## 31. How to fetch data from multiple collections (userList, order, productList) in a single query?
   - userList: {_id, name, email}
   - order: {_id, userId, productId, quantity, orderDate}
   - productList: {_id, name, price, category}

## 32. What is MongoDB Aggregation Pipeline? Explain stages.

## 33. What are MongoDB Indexes? Why are they important for performance?

## 34. How to create indexes in MongoDB? What types exist?

## 35. How to implement pagination in MongoDB (with and without aggregation)?

## 36. What is the difference between find() and aggregate()?

## 37. How to optimize MongoDB queries for better performance?

## 38. What are MongoDB Transactions? When to use them?

## 39. What is Soft Delete? How to implement it?

## 40. How to implement search functionality in MongoDB?

## 41. What is the difference between embedding and referencing in MongoDB?

## 42. How to perform case-insensitive search in MongoDB?

## 43. What is $lookup in MongoDB? How to use it?

## 44. What is $unwind in aggregation? When to use it?

## 45. What is $group in aggregation? Give example.

## 46. What is $match in aggregation pipeline?

## 47. What is $project in aggregation? How is it different from .select()?

## 48. How to join 2 or more collections in MongoDB?

## 49. What are aggregation operators? ($sum, $avg, $min, $max, etc.)

## 50. How to handle MongoDB connection in production?

## 51. What is connection pooling in MongoDB?

## 52. What is the difference between $lookup and $graphLookup?

## 53. How to count documents efficiently in MongoDB?

## 54. What is the difference between deleteOne(), deleteMany(), and findOneAndDelete()?

## 55. How to update nested objects in MongoDB?

## 56. How to update arrays in MongoDB documents?

## 57. What are MongoDB Schema Design patterns?

## 58. What is MongoDB Replication?

## 59. What is MongoDB Sharding?

## 60. How to handle duplicate key errors in MongoDB?

## 61. What is $addFields and $set in aggregation?

## 62. How to implement sorting with pagination?

## 63. What is the difference between save() and create() in Mongoose?

## 64. What is the difference between insertOne() and insertMany()?

## 65. How to implement full-text search in MongoDB?

## 66. What are TTL (Time To Live) indexes? When to use them?

## 67. How to perform bulk operations in MongoDB?

## 68. What is $facet in aggregation? Give use case.

## 69. What is the difference between lean() and toObject() in Mongoose?

## 70. How to implement autocomplete/suggest search in MongoDB?

## 71. What are the best practices for MongoDB schema design?

## 72. How to filter nested arrays in MongoDB?

## 73. How to use $push and $addToSet operators?

## 74. What is the difference between $push and $pull?

## 75. How to implement role-based access control in MongoDB?

## 76. What is $bucket and $bucketAuto in aggregation?

## 77. How to handle MongoDB connection failures in production?

## 78. What is the use of .explain() in MongoDB?

## 79. How to check query performance in MongoDB?

## 80. What is the difference between unique index and sparse index?

## 81. How to create compound indexes in MongoDB?

## 82. What is the use of $regex operator?

## 83. How to implement search, filter, sort, pagination in single API?

## 84. What are Mongoose middleware/hooks? (pre, post)

## 85. How to validate data in Mongoose schema?

## 86. What is populate() in Mongoose? How does it work?

## 87. What is the difference between $eq, $ne, $gt, $gte, $lt, $lte?

## 88. How to use $or and $and operators in MongoDB?

## 89. What is the use of $in and $nin operators?

## 90. How to handle pagination for large datasets efficiently?

---

## ADVANCED LEVEL

## 91. What is WiredTiger storage engine in MongoDB? How does it work?

## 92. Explain MongoDB Replica Set architecture (Primary, Secondary, Arbiter).

## 93. How does MongoDB election process work when primary goes down?

## 94. What are Write Concerns in MongoDB? (w:1, w:majority, w:0)

## 95. What are Read Concerns in MongoDB? (local, majority, linearizable)

## 96. What are Read Preferences in MongoDB? (primary, secondary, nearest)

## 97. How does MongoDB Sharding work? Explain shard key selection.

## 98. What is the difference between Range-based and Hash-based sharding?

## 99. What are Chunks in MongoDB sharding? How does chunk migration work?

## 100. What is a Config Server in MongoDB sharded cluster?

## 101. What is mongos router in MongoDB? How does it work?

## 102. How to design a shard key for high cardinality and even distribution?

## 103. What are the problems with monotonically increasing shard keys?

## 104. What is Change Streams in MongoDB? How to implement real-time updates?

## 105. How to implement event-driven architecture using Change Streams?

## 106. What is Oplog in MongoDB? How is it used in replication?

## 107. What is the Oplog size limit? How to configure it?

## 108. How to implement MongoDB caching with Redis?

## 109. What is the difference between in-memory storage and WiredTiger?

## 110. How to implement database migration in MongoDB?

## 111. What is MongoDB Atlas? Explain its features.

## 112. How to set up MongoDB Atlas cluster for production?

## 113. What are MongoDB Atlas triggers and functions?

## 114. How to implement full-text search with Atlas Search?

## 115. What is the difference between $text search and Atlas Search?

## 116. How to implement geospatial queries in MongoDB? (2d, 2dsphere indexes)

## 117. How to find nearby locations using $near and $geoWithin?

## 118. What is MapReduce in MongoDB? When to use it vs Aggregation?

## 119. How to implement data archiving strategy in MongoDB?

## 120. What are Capped Collections in MongoDB? When to use them?

## 121. How to implement audit logging in MongoDB?

## 122. What are MongoDB security best practices?

## 123. How to implement field-level encryption in MongoDB?

## 124. What is Client-Side Field Level Encryption (CSFLE)?

## 125. How to implement MongoDB role-based authentication?

## 126. What are MongoDB built-in roles? (read, readWrite, dbAdmin, etc.)

## 127. How to create custom roles in MongoDB?

## 128. How to enable and configure MongoDB authentication?

## 129. What is SCRAM authentication in MongoDB?

## 130. How to implement LDAP authentication with MongoDB?

## 131. How to backup and restore MongoDB database?

## 132. What is the difference between mongodump and mongorestore vs file system snapshots?

## 133. How to implement point-in-time recovery in MongoDB?

## 134. What is journaling in MongoDB? How does it ensure durability?

## 135. How to monitor MongoDB performance? (mongostat, mongotop)

## 136. What metrics to monitor in MongoDB production environment?

## 137. How to identify and fix slow queries in MongoDB?

## 138. What is the MongoDB profiler? How to use it?

## 139. How to analyze query plans using explain("executionStats")?

## 140. What are covered queries in MongoDB? How to optimize for them?

## 141. How to implement horizontal scaling in MongoDB?

## 142. What is the CAP theorem? Where does MongoDB fit?

## 143. How does MongoDB handle consistency vs availability trade-offs?

## 144. What is eventual consistency in MongoDB replication?

## 145. How to handle split-brain scenario in MongoDB?

## 146. What are MongoDB limits? (document size, nesting depth, etc.)

## 147. How to handle large files in MongoDB using GridFS?

## 148. What is the difference between GridFS and storing files as BSON?

## 149. How to implement data partitioning strategies in MongoDB?

## 150. What is zone sharding in MongoDB? How to implement it?

## 151. How to migrate from standalone MongoDB to replica set?

## 152. How to migrate from replica set to sharded cluster?

## 153. How to implement zero-downtime deployment with MongoDB?

## 154. What are the best practices for MongoDB schema versioning?

## 155. How to handle schema migrations in production MongoDB?

## 156. What is $merge stage in aggregation? How to use it for materialized views?

## 157. What is $out stage in aggregation? Difference from $merge?

## 158. How to implement materialized views in MongoDB?

## 159. What are Wildcard indexes in MongoDB? When to use them?

## 160. What is the difference between multikey index and compound index?

## 161. How to implement time-series data storage in MongoDB?

## 162. What are Time-Series Collections in MongoDB 5.0+?

## 163. How to optimize aggregation pipeline for large datasets?

## 164. What is allowDiskUse in aggregation? When to use it?

## 165. How to implement cursor-based pagination for better performance?

## 166. What is the difference between skip-limit and cursor-based pagination?

## 167. How to handle concurrent writes in MongoDB?

## 168. What is Optimistic Locking in MongoDB? How to implement it?

## 169. What is the difference between findAndModify and updateOne?

## 170. How to implement distributed locks using MongoDB?

## 171. What is $sample stage in aggregation? Use cases?

## 172. How to implement random document selection efficiently?

## 173. What are $expr and $function in aggregation?

## 174. How to use JavaScript in MongoDB aggregation ($function, $accumulator)?

## 175. What is the performance impact of using JavaScript in queries?

## 176. How to implement multi-tenant architecture in MongoDB?

## 177. What are the strategies for multi-tenancy? (shared DB, separate DB, hybrid)

## 178. How to handle cross-shard transactions in MongoDB?

## 179. What are the limitations of MongoDB transactions?

## 180. How to implement saga pattern for distributed transactions?

## 181. What is MongoDB Realm? How does it work with mobile apps?

## 182. How to implement offline-first sync with MongoDB Realm?

## 183. What are aggregation pipeline optimizations done by MongoDB internally?

## 184. How to use $planCacheStats to analyze query plans?

## 185. What is the query plan cache in MongoDB?

## 186. How to clear and rebuild indexes in MongoDB?

## 187. What is index intersection in MongoDB?

## 188. How to implement database-per-service pattern with MongoDB?

## 189. What are the challenges of microservices with MongoDB?

## 190. How to implement event sourcing with MongoDB?
